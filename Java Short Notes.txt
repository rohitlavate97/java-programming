Features of Java:
----------------
1. Platform Independence: Java code is compiled into bytecode, which is executed by the JVM.
2. Object-Oriented: Java follows the object-oriented programming paradigm.
3. Garbage Collection: Java has an automatic garbage collection mechanism that manages memory
4. Robustness: Java's strong type-checking, exception handling, and memory management contribute to its robustness.
5. Security: Java provides a secure execution environment by using a sandbox model for applets and built-in security mechanisms.
6. Multi-threading Support: Java has built-in support for concurrent programming through threads, enabling the creation of multithreaded applications.
7. Rich Standard Library: Java comes with a vast standard library that provides a wide range of functionalities, making development faster and more efficient.
8. High Performance: Although Java is an interpreted language, its bytecode is compiled at runtime by the JVM, resulting in high performance

-public static void main(String[] args)
--------------------------------------
The public keyword indicates that the class is accessible from other classes.
The static keyword is used to define a static method that belongs to the class itself, not to any specific instance of the class.
The void keyword indicates that the main method does not return any value.
main is the entry point of the program. It is the method that will be executed when we run the program.
The String[] args is an array of strings representing the command-line arguments passed to the program.

-Program Internal: Internal structure of a Java program
-----------------
        +----------------------+
        |   Java Source Code   |
        |      (.java file)    |
        +----------------------+
                   |
                   |  javac (Java Compiler)
                   v
        +----------------------+
        |     Bytecode         |  Bytecode contains the instruction that only JVM understands
        |     (.class file)    |
        +----------------------+
                   |
                   |  Loaded by JVM (the JVM loads the necessary classes and starts executing the main method of the specified class )
                   v
        +----------------------+
        |   Java Virtual       |
        |   Machine (JVM)      |
        +----------------------+
                   |
                   |  Executes Bytecode
                   v
        +----------------------+
        |   Program Output     |
        | (Console/Application)|
        +----------------------+
		
-Java Variables: variables are used to store data values that can be manipulated within a program. 
---------------
Before using a variable, it needs to be declared with a specific data type.
1. Primitive Data Types: These are basic data types built into the language.
                         Numeric Types: byte , short , int , long , float , and double .
                         Characters: char .
                         Boolean: boolean .
byte : 8-bit signed integer. Range: -128 to 127.
short : 16-bit signed integer. Range: -32,768 to 32,767.
int : 32-bit signed integer. Range: -2^31 to 2^31 - 1.
long : 64-bit signed integer. Range: -2^63 to 2^63 - 1.
float : 32-bit floating-point. Can represent decimal numbers.
double : 64-bit floating-point. More precise than float .
char : 16-bit Unicode character. Represents single characters.
boolean : Represents true or false .
2. Reference Data Types: These types are references to objects in memory.
                         Examples: String , user-defined classes, arrays, etc.

Unicode System: Unicode is a character encoding standard that aims to support all the characters from all the writing systems in the world.
--------------
char unicodeChar = '\\u0041'; // Represents the character 'A' ->The escape sequence \\u is used to indicate a Unicode character, followed by the four-digit hexadecimal representation of the Unicode code point.

Operators: used to perform operations on variables and values.
---------
Arithmetic Operators: + (addition), (subtraction), (multiplication), /(division), % (modulus).
Assignment Operators: = , += , -= , = , /= , %= .
Increment and Decrement Operators: ++ (increment), - (decrement).
Relational Operators: == (equals), != (not equals), > , < , >= , <= .
Logical Operators: && (logical AND), || (logical OR), ! (logical NOT).
Bitwise Operators: & (bitwise AND), | (bitwise OR), ^ (bitwise XOR), ~ (bitwise NOT).

Keywords:
--------
-Keywords in Java are reserved words that have predefined meanings and cannot be used as identifiers (e.g., variable names, class names).
-abstract, boolean, break, byte, case, catch, class, continue, default, do, double,else, extends, final, finally, float, for, 
if, implements, import, instanceof,int, interface, long, new, package, private, protected, public, return, short,static, super, 
switch, this, throw, throws, try, void, while

Java Control Statements:
-----------------------
Control statements in Java are used to control the flow of a program's execution based on certain conditions or iterations. They help make decisions and repeat
actions as needed.
-Java If-else:
-Java Switch: The switch statement is used to perform different actions based on the value of a variable or an expression. 
It provides an alternative to using multiple if-else statements.
int dayOfWeek = 3;
switch (dayOfWeek) {
case 1:
System.out.println("Monday");
break;
case 2:
System.out.println("Tuesday");
break;
default:
System.out.println("Invalid day");
}
-Java For Loop: execute a block of code repeatedly for a fixed number of times
-Java While Loop:executes a block of code as long as the given condition is true.
-Java Do-While Loop: The do-while loop is similar to the while loop, but it ensures that the block of code is executed at least once before checking the condition.
-Java Break:The break statement is used to terminate the loop or switch statement prematurely, based on a condition.
-Java Continue:The continue statement is used to skip the current iteration and continue with the next iteration of a loop based on a condition.

-Javadoc comments: are used for generating documentation. 
-----------------
They start with /** and end with / . Javadoc comments are placed before classes, methods, or fields and can include additional tags for 
documentation generation:
/**
* This method calculates the sum of two numbers.
* @param a The first number.
* @param b The second number.
* @return The sum of a and b.
*/
public int sum(int a, int b) {
return a + b;
}

-Java Object Class:
------------------
In Java, the Object class is the root class of all other classes. It is defined in the java.lang package and is automatically inherited by all other classes,
either directly or indirectly. Every class in Java is a subclass of the Object class.
The Object class provides several important methods that are inherited by all classes, such as equals() , hashCode() , toString() , getClass() , and more. 
These methods can be overridden in user-defined classes to provide custom behavior.

-Java OOPs Concepts:
-------------------
1. Encapsulation: Encapsulation is the bundling of data (attributes) and methods that operate on that data within a single unit, called a class. 
It helps in data hiding and prevents direct access to the internal data from outside the class.
2. Inheritance: Inheritance allows a class (subclass) to inherit the properties and behaviors of another class (superclass). 
It promotes code reusability and allows new classes to extend existing classes.
3. Polymorphism: Polymorphism allows an object to take on multiple forms. It can be achieved through method overloading (compile-time polymorphism) and
method overriding (run-time polymorphism).
4. Abstraction: Abstraction involves the creation of abstract classes and interfaces, which define the structure and behavior of classes without specifying
their implementation details.
5. Association: Association represents a relationship between two classes, where one class uses the functionalities provided by another class.
6. Aggregation: Aggregation is a special form of association that represents a "has-a" relationship. It implies a relationship where one class 
contains another class as part of its structure. A "has-a" relationship where one class contains another class, but both can exist independently. 
ex: Department has Teachers
class Address
{
    String street;
    String city;

    Address(String street, String city)
    {
        this.street = street;
        this.city = city;
    }
}
→ Address class stores address details.
class Person
{
    String name;
    Address address;

    Person(String name, Address address)
    {
        this.name = name;
        this.address = address;
    }
}
→ Person class contains Address object.
→ This represents Aggregation ("has-a" relationship).
Object Creation Example:
------------------------
Address addr = new Address("MG Road", "Nashik");
Person p = new Person("Rohit", addr);
7. Composition: Composition is a stronger form of aggregation, where one class is composed of one or more other classes. The composed classes cannot exist
independently Ex: House has Rooms (If House is destroyed, Rooms also cease to exist.)

-Naming Convention:
------------------
1. Class Names: CamelCasing
2. Variable Names: Start with a lowercase letter
3. Constant Names: Use uppercase letters,separate words with underscores.
4. Method Names: Start with a lowercase letter

-Object and Class:
-----------------
-A class is a blueprint or a template for creating objects. It defines the attributes (fields) and behaviors (methods) that the objects of the class will have. 
-An object is an instance of a class, representing a specific entity based on the class definition.

-Method: A method in Java is a block of code that defines the behavior of an object
-------
Constructor:A constructor in Java is a special method used to initialize objects of a class. It has the same name as the class and 
-----------
is called automatically when an object is created. Constructors are used to set initial values for the object's attributes or perform other setup tasks.

-static Keyword: The static keyword is used to declare members (variables and methods) that belong to the class itself, rather than to individual objects. 
---------------
They are shared by all objects of the class and can be accessed directly using the class name, without creating an instance of the class

-this Keyword: The this keyword in Java is a reference to the current instance of the class. 
-------------
It is used to distinguish between instance variables and parameters with the same name, and to access instance variables and methods within the class
Ex:
public class Person {
 String name;
 public void setName(String name) {
  this.name = name;
 }
}


-Java Inheritance: allows one class (subclass or derived class) to inherit the properties and behaviors of another class (superclass or base class). 
-----------------
The subclass can extend the functionality of the superclass by adding new attributes and methods or by overriding existing ones.
Inheritance represents an IS-A relationship between classes
Ex: Dog is a subclass of Animal . So, a Dog IS-A Animal . When we create a Dog object, it can call methods defined in both Dog and Animal .
Animal animal = new Dog();
animal.makeSound(); // Output: Dog barks.

In summary, inheritance (IS-A) is used to achieve code reusability and represents a relationship between a superclass and its subclasses, 
while aggregation (HAS-A) represents a relationship between a whole class and its parts, allowing one class to contain another as an attribute. 
Both concepts are essential in designing objectoriented systems and promoting modularity and flexibility in code.

-Java Polymorphism:allows objects of different classes to be treated as objects of a common superclass. 
------------------
It provides a way to perform a single action in different ways based on the context.
There are two main types of polymorphism in Java: compile-time polymorphism and runtime polymorphism.
-Method overloading is an example of compile-time (static) polymorphism. It allows a class to have multiple methods with the same name but different parameters. The
compiler determines which method to call based on the number or types of arguments passed.
class MathUtils
{
    int add(int a, int b)
    {
        return a + b;
    }

    double add(double a, double b)
    {
        return a + b;
    }
}
-Method overriding is an example of runtime (dynamic) polymorphism. It occurs when a subclass provides a specific implementation for a method that 
is already defined in its superclass. The method in the subclass must have the same name, return type, and parameters as the method in the superclass.

-Covariant Return Type: Covariant return type is a feature introduced in Java 5 that allows a subclass to override a method in its superclass with a more specific return type. 
----------------------
The return type in the subclass method can be a subclass of the return type in the superclass method.
class Animal
{
    Animal getAnimal()
    {
        return this;
    }
}

class Dog extends Animal
{
    Dog getAnimal()
    {
        return this;
    }
}

-super Keyword:The super keyword is used to refer to the superclass's members (fields, methods, and constructors) from the subclass. 
--------------
It is often used to call the superclass's constructor or to access overridden methods or fields.

class Animal
{
    String name;
    Animal(String name)
    {
        this.name = name;
    }
}

class Dog extends Animal
{
    String breed;
    Dog(String name, String breed)
    {
        super(name);   // Calls Animal constructor
        this.breed = breed;
    }
}

- INSTANCE INITIALIZER BLOCK:An Instance Initializer Block is a block of code inside a class that executes whenever an object is created.
----------------------------
It runs before the constructor

Syntax:
class Demo
{
    // Instance Initializer Block
    {
        System.out.println("Initializer Block Executed");
    }

    Demo()
    {
        System.out.println("Constructor Executed");
    }
}
Important Points:
-----------------
✔ Executes every time an object is created.
✔ Runs before constructor.
✔ Used for common initialization code.
✔ Multiple initializer blocks are allowed.
✔ Executed in the order they appear in class.

Difference:
------------
Instance Initializer Block
→ Executes for every object creation.

Static Block
→ Executes only once when class loads.

-final Keyword:The final keyword is used to declare that a variable, method, or class cannot be changed or overridden after its initialization or definition.
--------------
Final Variable: Once initialized, the value of a final variable cannot be modified.
Final Method: A final method cannot be overridden by subclasses.
Final Class: A final class cannot be subclassed.

-Runtime Polymorphism:
---------------------
Runtime polymorphism is also known as dynamic polymorphism or late binding. It occurs when the method to be executed is determined at runtime based on the
actual type of the object, not at compile time.(Ex:method overriding)

-Dynamic Binding:Dynamic binding is the process of linking a method call to its method definition during runtime. 
----------------
It allows Java to support runtime polymorphism and method overriding. When a method is overridden in a subclass, the actual method to be called is
determined dynamically based on the type of the object at runtime.

-instanceof Operator: The instanceof operator is used to check whether an object belongs to a specific class or its subclasses.
--------------------
It returns true if the object is an instance of the specified class or false otherwise.
EX:
class Animal { }

class Dog extends Animal { }

public class Main
{
    public static void main(String[] args)
    {
        Animal animal = new Dog();
        if(animal instanceof Dog)
        {
            System.out.println("The animal is a Dog.");
        }
    }
}

-Java Abstraction: The process of hiding the implementation details of an object and exposing only the relevant features or behaviors to the outside world.
-----------------
It allows us to focus on what an object does rather than how it does it. Abstraction is achieved in Java through abstract classes and interfaces.

-Abstract Class:An abstract class is a class that cannot be instantiated directly and is intended to be subclassed. 
---------------
It serves as a blueprint for other classes and can contain abstract methods (methods without a body) that must be implemented by its subclasses. 
An abstract class can also have concrete methods with an implementation.
Important Points:
-----------------
✔ Abstract class cannot create objects.
✔ Can contain abstract and normal methods.
✔ Abstract methods must be overridden.
✔ Used for achieving abstraction.

abstract class Shape
{
    int x, y;

    abstract void draw();

    void moveTo(int newX, int newY)
    {
        this.x = newX;
        this.y = newY;
    }
}

class Circle extends Shape
{
    void draw()
    {
        System.out.println("Drawing Circle");
    }
}

-Interface: An interface in Java is a blueprint of a class that defines a contract for the classes that implement it. 
----------
It contains only abstract methods and constants (implicitly public , static , and final ). Interfaces are used to achieve multiple inheritance in Java.
Important Points:
-----------------
✔ Interface provides full abstraction.
✔ Methods are public abstract by default.
✔ Variables are public static final by default.
✔ Supports multiple inheritance.
✔ Class uses implements keyword.
✔ Interface variables are constants. Values cannot be modified.

interface Drawable
{
    void draw();

    int MAX_WIDTH  = 800;
    int MAX_HEIGHT = 600;
}

1. Abstract Method
------------------

void draw();

By default becomes:

public abstract void draw();


2. Constants
------------

int MAX_WIDTH  = 800;
int MAX_HEIGHT = 600;

By default become:

public static final int MAX_WIDTH  = 800;
public static final int MAX_HEIGHT = 600;

| Feature                   | Abstract Class                                                      | Interface                                      |
| ------------------------- | ------------------------------------------------------------------- | ---------------------------------------------- |
| **Instantiation**         | Cannot be instantiated directly                                     | Cannot be instantiated at all                  |
| **Constructor**           | Can have constructors(called when a subclass is instantiated)       | Cannot have constructors                       |
| **Methods**               | Can contain both abstract and concrete methods                      | Mainly contains abstract methods               |
| **Java 8 Features**       | Normal methods allowed                                              | Can contain default and static methods         |
| **Inheritance**           | A class can extend only one abstract class                          | A class can implement multiple interfaces      |
| **Type of Inheritance**   | Supports single inheritance                                         | Supports multiple inheritance                  |
| **Fields / Variables**    | Can have instance variables                                         | Variables are implicitly `public static final` |
| **Access Modifiers**      | Members can be `public`, `protected`, `private`, or package-private | Members are implicitly `public`                |
| **Purpose**               | Used to provide common base and shared behavior                     | Used to define a contract/rules                |
| **Relationship**          | Suitable for closely related classes                                | Suitable for unrelated classes                 |
| **Method Implementation** | Methods may or may not have implementation                          | Methods are usually abstract                   |
| **Example Keyword**       | `extends`                                                           | `implements`                                   |

+-------------------------------------------------------------+
|                 USE ABSTRACT CLASS WHEN:                    |
+-------------------------------------------------------------+
| ✔ You want common functionality for related classes.        |
| ✔ You need shared code implementation.                      |
| ✔ Classes have "is-a" relationship.                         |
| ✔ You want constructors and instance variables.             |
| ✔ You want partial abstraction.                             |
+-------------------------------------------------------------+

+-------------------------------------------------------------+
|                    USE INTERFACE WHEN:                      |
+-------------------------------------------------------------+
| ✔ You want to define rules/contracts.                       |
| ✔ Unrelated classes must follow same behavior.              |
| ✔ You need multiple inheritance.                            |
| ✔ You want full abstraction.                                |
+-------------------------------------------------------------+

-Package: A package in Java is a mechanism to organize related classes and interfaces into a single unit. 
---------

-Access Modifiers: Access modifiers control the visibility and accessibility of classes, variables, methods, and constructors in Java. 
-----------------
There are four types of access modifiers:
1. public: accessible from any class in any package.
2. protected: accessible within the same package and also by subclasses (even if they are in a different package).
3. default (package-private): Members with no access modifier (i.e., no public ,protected , or private keyword) are accessible only within the same package.
4. private: accessible only within the same class

-Java Encapsulation:
-------------------
closely related to the concept of data hiding. It refers to the bundling of data (attributes) and methods that operate on that data within 
a single unit (i.e., a class).The data is hidden or made private within the class, and access to the data is controlled through public methods, 
providing a layer of abstraction.
Encapsulation is achieved in Java by using access modifiers to control the access to the attributes (data) and methods of a class. 
By making the attributes private and providing public getter and setter methods, we can ensure that the data is accessed and modified in a controlled manner.
The main goals of encapsulation are to:
1. Hide the internal details of how an object's data is represented and manipulated.
2. Protect the data from unauthorized access or modification by external code.
3. Provide controlled access to the data through getter and setter methods.

By using encapsulation, we can ensure that the internal state of an object is protected from unauthorized access and modification, promoting better code
maintainability and reusability. Clients of the class interact with the object through its public methods, and the internal details remain hidden from them.

public class Person
{
    private String name;
    private int age;

    public String getName()
    {
        return name;
    }
    public void setName(String name)
    {
        this.name = name;
    }
    public int getAge()
    {
        return age;
    }
    public void setAge(int age)
    {
        if(age >= 0)
        {
            this.age = age;
        }
        else
        {
            System.out.println(
            "Invalid age. Age must be non-negative.");
        }
    }
}

-Java Array:An array in Java is a collection of elements of the same data type that are stored in contiguous memory locations. 
-----------
Arrays provide a way to store multiple values under a single variable name, making it easier to manage and access data. Each element in an array is 
identified by its index, starting from 0.
Types of Array in Java: Single-Dimensional Array,Multidimensional Array
-A single-dimensional array in Java is declared using square brackets [] after the data type, followed by the array name.
// Declare a single-dimensional array to store integers
int[] numbers;
-A multidimensional array in Java is an array of arrays. It can have multiple dimensions, such as 2D or 3D arrays.
// Declare a 2D array to store integers
int[][] matrix;
// Declare a 3D array to store characters
char[][][] cube;
Ex:
public class ArrayExample
{
    public static void main(String[] args)
    {
        // Declare array
		//Array declaration specifies:
        //✔ Data type of elements
        //✔ Array reference name
        int[] numbers;
        // Create array with size 5, instantiate
		//Instantiation creates the array object
        //and allocates memory using new keyword.
        numbers = new int[5];
        // Initialize array elements -> Assigning values to array elements using index positions.
        numbers[0] = 10;
        numbers[1] = 20;
        numbers[2] = 30;
        numbers[3] = 40;
        numbers[4] = 50;
        // Print array elements
        for(int i = 0; i < numbers.length; i++)
        {
            System.out.println(
            "Element at index " + i + ": " + numbers[i]);
        }
    }
}

Alternate Way
int[] numbers = {10, 20, 30, 40, 50};   //Array Declaration + Array Creation + Array Initialization

-Object Cloning:Object cloning in Java is the process of creating a copy of an object. 
---------------
-The Cloneable interface, available in the java.lang package, is used to indicate that a class supports cloning. 
-The clone() method is used to create  a shallow copy of the object.
-To perform deep cloning (i.e., copying both the object and its internal references), additional customization is needed.
Ex:
class Student implements Cloneable{
    private String name;

    public Student(String name)
    {
        this.name = name;
    }

    @Override
    protected Object clone()
    throws CloneNotSupportedException
    {
        return super.clone();
    }

    @Override
    public String toString()
    {
        return "Student: " + name;
    }
}

public class ObjectCloningExample
{
    public static void main(String[] args)
    {
        try
        {
            Student student1 = new Student("John");

            Student student2 =
            (Student) student1.clone();

            System.out.println(
            "Original Object: " + student1);

            System.out.println(
            "Cloned Object: " + student2);
        }
        catch(CloneNotSupportedException e)
        {
            e.printStackTrace();
        }
    }
}


Important Points:
-----------------
✔ Cloneable is marker interface.
✔ clone() creates object copy.
✔ super.clone() performs shallow copy.
✔ Cloned object has separate memory.
✔ Type casting needed because clone() returns Object type.

-Math class:part of the java.lang package and provides several static methods for common mathematical operations.
-----------
includes basic arithmetic operations (e.g., add , subtract , multiply , divide ) as well as methods for more complex mathematical functions 
Important Points:
-----------------
✔ Math class belongs to java.lang package.
✔ No need to import java.lang.
✔ Methods are static.
✔ Object creation not required.
✔ Used for mathematical calculations.
+----------------------------------------------+
| Method               | Description           |
+----------------------------------------------+
| Math.addExact()      | Addition              |
| Math.subtractExact() | Subtraction           |
| Math.multiplyExact() | Multiplication        |
| Math.divideExact()   | Division              |
| Math.sqrt()          | Square root           |
| Math.pow()        | Power calculation        |
| Math.sin()        | Sine value               |
| Math.cos()        | Cosine value             |
| Math.log()        | Natural logarithm        |
| Math.max()        | Maximum value            |
| Math.min()        | Minimum value            |
| Math.random()    | Random number             |
+----------------------------------------------+

public class MathClassExample
{
    public static void main(String[] args)
    {
        int x = 5;
        int y = 3;

        // Basic arithmetic operations
        int sum = Math.addExact(x, y);
        int difference = Math.subtractExact(x, y);
        int product = Math.multiplyExact(x, y);
        int quotient = Math.floorDiv(x, y);
        int remainder = Math.floorMod(x, y);

        // Complex mathematical functions
        double squareRoot = Math.sqrt(x);
        double power = Math.pow(x, y);
        double sinValue =
        Math.sin(Math.toRadians(30));

        double cosValue =
        Math.cos(Math.toRadians(60));

        double logValue = Math.log(x);
    }
}

-Wrapper Class:
--------------
Collections and many Java utilities work only with objects, not primitives.
Wrapper classes help:
✔ Convert primitive → object
✔ Convert object → primitive

+----------------------------------+
| Primitive Type | Wrapper Class   |
+----------------------------------+
| byte           | Byte            |
| short          | Short           |
| int            | Integer         |
| long           | Long            |
| float          | Float           |
| double         | Double          |
| char           | Character       |
| boolean        | Boolean         |
+----------------------------------+
Important Points:
-----------------
✔ Wrapper classes belong to java.lang package.
✔ Used in Collections Framework.
✔ Supports object-oriented features.
✔ Enables autoboxing and unboxing.
✔ Helps convert between primitive and object.
Conversion Types:
-----------------
1. Boxing
-----------
Primitive → Object

int x = 5;
Integer obj = Integer.valueOf(x);

2. Unboxing
-------------
Object → Primitive

Integer obj = 10;
int x = obj;

+-------------+      Boxing      +----------------+
|  int = 10   |  ------------->  | Integer Object |
+-------------+                  |  value = 10    |
                                 +----------------+
								 
public class WrapperClassExample {

    public static void main(String[] args) {

        // Using primitive data types
        int intValue = 10;
        double doubleValue = 3.14;
        boolean booleanValue = true;

        // Using wrapper classes
        Integer integerObj = Integer.valueOf(intValue);
        Double doubleObj = Double.valueOf(doubleValue);
        Boolean booleanObj = Boolean.valueOf(booleanValue);

        // Auto-boxing
        Integer anotherIntegerObj = intValue;
        Double anotherDoubleObj = doubleValue;
        Boolean anotherBooleanObj = booleanValue;

        // Unboxing
        int unboxedIntValue = integerObj;
        double unboxedDoubleValue = doubleObj;
        boolean unboxedBooleanValue = booleanObj;

        System.out.println("Using Wrapper Classes:");
        System.out.println("Integer Object: " + integerObj);
        System.out.println("Double Object: " + doubleObj);
        System.out.println("Boolean Object: " + booleanObj);

        System.out.println("\nAuto-boxing:");
        System.out.println("Another Integer Object: " + anotherIntegerObj);
        System.out.println("Another Double Object: " + anotherDoubleObj);
        System.out.println("Another Boolean Object: " + anotherBooleanObj);

        System.out.println("\nUnboxed Values:");
        System.out.println("Unboxed Int Value: " + unboxedIntValue);
        System.out.println("Unboxed Double Value: " + unboxedDoubleValue);
        System.out.println("Unboxed Boolean Value: " + unboxedBooleanValue);
    }
}

-Java Recursion: Recursion is a programming technique where a method calls itself to solve a problem
---------------
Recursion can be used to solve problems that can be broken down into smaller, similar subproblems(Ex: factorial or Fibonacci series.)
EX:
public class RecursionExample
{
    // Factorial using recursion
    public static int factorial(int n)
    {
        if(n == 0 || n == 1)
        {
            return 1;
        }
        else
        {
            return n * factorial(n - 1);
        }
    }
}
Important Points:
-----------------
✔ Recursion means method calling itself.
✔ Base condition is mandatory.
✔ Recursive calls use stack memory.
✔ Useful for factorial, Fibonacci, tree traversal.

-Call By Value:Passing copies of values/references to methods.
--------------
✔ Java always uses Call By Value.
✔ Primitive values are copied directly. Original variable/reference remains unchanged.
✔ Object references are copied.
✔ Object data can be modified.
✔ Original reference cannot be replaced.

class Student
{
    String name;
}

public class Test
{
    static void changeName(Student s)
    {
        s.name = "John";
    }

    public static void main(String[] args)
    {
        Student st = new Student();

        st.name = "Rohit";

        changeName(st);

        System.out.println(st.name);
    }
}
 REFERENCE CHANGE EXAMPLE
======================================================

Example:
--------
static void change(Student s)
{
    s = new Student();

    s.name = "Amit";
}

Explanation:
------------
Only local copy of reference changes.
Original object remains unchanged.

- strictfp keyword:
------------------
✔ Used for floating-point consistency.
✔ Affects float and double calculations.
✔ Ensures platform-independent results.
✔ Follows IEEE 754 standard strictly.
✔ Mostly used in scientific calculations.

public strictfp class StrictfpExample
{
    public static void main(String[] args)
    {
        double num1 = 10.0;
        double num2 = 3.0;

        double result = num1 / num2;

        System.out.println(result);
    }
}

-javadoc tool:
-------------
The javadoc tool is a utility in Java used to generate HTML documentation from Java source code. It parses the source code, 
extracts the comments written in the Javadoc format, and generates API documentation in HTML format. To generate Javadoc documentation, 
you need to include Javadoc comments in your source code and run the javadoc command from the command line. 

-Command Line Arg:
-----------------
Java programs can access command-line arguments through the args parameter in the main method. The args parameter is an array of strings, 
where each element represents a command-line argument.

-Object vs. Class:
-----------------
Class: A class is a blueprint or template for creating objects. It defines the structure and behavior of objects but does not occupy memory space. 
It is like a user-defined data type.
Object: An object is an instance of a class. It is a runtime entity that occupies memory and can be used to access the members 
(fields and methods) defined in its class.

-Overloading vs. Overriding:
---------------------------
Overloading: Overloading is a concept in Java that allows a class to have multiple methods with the same name but different parameters. The methods
must have different method signatures, i.e., the number or types of parameters should be different. Overloading is a form of compile-time polymorphism.
Overriding: Overriding is a concept in Java that occurs when a subclass provides a specific implementation for a method that is already defined in 
its superclass. The method in the subclass must have the same name, return type, and parameters as the method in the superclass. Overriding is a form 
of runtime polymorphism.

class Animal {
    public void makeSound() {
        System.out.println("Animal makes a sound");
    }
}

class Dog extends Animal {

    // Method Overriding
    @Override
    public void makeSound() {
        System.out.println("Dog barks");
    }

    // Method Overloading
    public void makeSound(String soundType) {
        System.out.println("Dog " + soundType);
    }
}

public class OverloadingOverridingExample {

    public static void main(String[] args) {

        Animal animal = new Animal();
        Animal dog = new Dog();

        // Method overriding - Runtime Polymorphism
        animal.makeSound(); // Animal version
        dog.makeSound();    // Dog version

        // Method overloading
        Dog myDog = new Dog();

        myDog.makeSound();          // No argument method
        myDog.makeSound("howls");   // String argument method
    }
}

--Java String: In Java, string is basically an object that represents sequence of char values. An array of characters works same as Java string.(java.lang)
-------------
String objects are immutable, which means that once a String is created, its value cannot be changed. Any modification to a String results in 
a new String object being created.
-CharSequence Interface: The CharSequence interface is a part of the java.lang package and is implemented by the String , 
StringBuilder , and StringBuffer classes in Java. It represents a sequence of characters and provides a read-only access to the character data. 
As it is an interface, it cannot be directly instantiated, but its implementing classes can be used to work with character sequences in Java.
How to create a String object?
There are two ways to create a String object in Java:
1. String Literal: A string literal is a sequence of characters enclosed in doublequotes. When you create a string using a string literal, 
Java automatically creates a String object in the string pool if it does not already exist.
String str1 = "Hello, World!";
2. By new keyword: You can also create a String object using the new keyword. This creates a new String object in the heap memory, even if a similar string
already exists in the string pool.
String str2 = new String("Java is fun!");

-Java String class methods:
--------------------------
| Method                                                         | Description                                           | Example                                         |
| -------------------------------------------------------------- | ----------------------------------------------------- | ----------------------------------------------- |
| `length()`                                                     | Returns length of string                              | `str.length()`                                  |
| `charAt(int index)`                                            | Returns character at index                            | `str.charAt(0)`                                 |
| `concat(String str)`                                           | Concatenates strings                                  | `str.concat(" World")`                          |
| `contains(CharSequence s)`                                     | Checks substring existence                            | `str.contains("Java")`                          |
| `equals(Object obj)`                                           | Compares content                                      | `str.equals("Hello")`                           |
| `equalsIgnoreCase(String str)`                                 | Case-insensitive comparison                           | `str.equalsIgnoreCase("hello java")`            |
| `startsWith(String prefix)`                                    | Checks starting text                                  | `str.startsWith("Hello")`                       |
| `endsWith(String suffix)`                                      | Checks ending text                                    | `str.endsWith("Java")`                          |
| `indexOf(String str)`                                          | Finds first occurrence                                | `str.indexOf("Java")`                           |
| `lastIndexOf(String str)`                                      | Finds last occurrence                                 | `str.lastIndexOf("a")`                          |
| `substring(int beginIndex)`                                    | Extracts part of string                               | `str.substring(6)`                              |
| `substring(int begin, int end)`                                | Extracts string between indexes                       | `str.substring(0, 5)`                           |
| `replace(char old, char new)`                                  | Replaces characters                                   | `str.replace('a', 'x')`                         |
| `replace(CharSequence old, CharSequence new)`                  | Replaces text                                         | `str.replace("Java", "Python")`                 |
| `replaceAll(String regex, String replacement)`                 | Replaces all substrings matching regex                | `str.replaceAll("a", "@")`                      |
| `toUpperCase()`                                                | Converts to uppercase                                 | `str.toUpperCase()`                             |
| `toLowerCase()`                                                | Converts to lowercase                                 | `str.toLowerCase()`                             |
| `trim()`                                                       | Removes spaces from ends                              | `str.trim()`                                    |
| `split(String regex)`                                          | Splits string into array                              | `str.split(" ")`                                |
| `isEmpty()`                                                    | Checks empty string                                   | `str.isEmpty()`                                 |
| `valueOf(int i)`                                               | Converts int to String                                | `String.valueOf(100)`                           |
| `valueOf(double d)`                                            | Converts double to String                             | `String.valueOf(10.5)`                          |
| `valueOf(char c)`                                              | Converts char to String                               | `String.valueOf('A')`                           |
| `intern()`                                                     | Returns canonical representation from string pool     | `str.intern()`                                  |
| `compareTo(String str)`                                        | Lexicographical comparison                            | `str.compareTo("Hello")`                        |
| `format(String format, Object... args)`                        | Formats a string using format specifiers              | `String.format("Name: %s Age: %d", "John", 25)` |
| `getBytes()`                                                   | Converts string into byte array using default charset | `str.getBytes()`                                |
| `getChars(int srcBegin, int srcEnd, char[] dst, int dstBegin)` | Copies characters into character array                | `str.getChars(0, 5, arr, 0)`                    |
| `join(CharSequence delimiter, CharSequence... elements)`       | Joins strings using delimiter                         | `String.join("-", "Java", "Python")`            |
| `toCharArray()`                                                | Converts string to character array                    | `str.toCharArray()`                             |


public class StringMethodsExample {

    public static void main(String[] args) {

        String str = "Hello, Java!";

        // Length of the string
        int length = str.length();
        System.out.println("Length of the string: " + length);

        // Get character at a specific index
        char ch = str.charAt(4);
        System.out.println("Character at index 4: " + ch);

        // Concatenate strings
        String concatStr = str.concat(" Welcome");
        System.out.println("Concatenated string: " + concatStr);

        // Check if the string contains a specific substring
        boolean containsJava = str.contains("Java");
        System.out.println("Contains 'Java': " + containsJava);

        // Check if the string starts with a specific prefix
        boolean startsWithHello = str.startsWith("Hello");
        System.out.println("Starts with 'Hello': " + startsWithHello);

        // Check if the string ends with a specific suffix
        boolean endsWithExclamation = str.endsWith("!");
        System.out.println("Ends with '!': " + endsWithExclamation);

        // Find the index of a substring
        int indexJava = str.indexOf("Java");
        System.out.println("Index of 'Java': " + indexJava);

        // Convert the string to uppercase and lowercase
        String uppercaseStr = str.toUpperCase();
        String lowercaseStr = str.toLowerCase();

        System.out.println("Uppercase: " + uppercaseStr);
        System.out.println("Lowercase: " + lowercaseStr);

        // Replace characters or substrings
        String replacedStr = str.replace("Java", "Python");
        System.out.println("Replaced string: " + replacedStr);

        // Split the string into an array of substrings
        String[] words = str.split(", ");

        for (String word : words) {
            System.out.println("Word: " + word);
        }

        // Remove leading and trailing whitespaces
        String trimmedStr = str.trim();
        System.out.println("Trimmed string: " + trimmedStr);
    }
}

String Comparison:
------------------
In Java, you can compare strings using the equals() method to check if the content of two strings is the same. For reference-based comparison, 
you can use the == operator, but it only checks if the two string references point to the same memory location, not the content.
== → compares references
equals() → compares content

Immutable String:
----------------
In Java, a String object is immutable, which means once it is created, its content cannot be changed. When you modify a String , 
a new String object is created with the modified content. The immutability of strings ensures their safety in multithreaded environments 
and allows for efficient string manipulation because the same string can be shared by multiple references without fear of modification.

String Concatenation:
--------------------
String concatenation in Java can be performed using the + operator or the concat() method of the String class. When concatenating multiple 
strings using the + operator, the Java compiler automatically converts the expression into a StringBuilder or StringBuffer object for efficiency.

Substring:
---------
The substring() method in Java is used to extract a portion of a string. It takes two parameters: the starting index (inclusive) and 
the ending index (exclusive) of the substring to be extracted.
String subStr1 = str.substring(0, 5);
System.out.println("Substring 1: " + subStr1);
// Extract a substring from index 7 to the end of the string
String subStr2 = str.substring(7);

StringBuffer class:
-------------------
The StringBuffer class in Java provides a mutable sequence of characters. It is similar to String but allows modification of the 
content without creating a new object. This makes StringBuffer suitable for situations where frequent string modifications are required, 
such as in a loop.
| Method             | Description                    | Example                  |
| ------------------ | ------------------------------ | ------------------------ |
| `append()`         | Adds text at end               | `sb.append(" Java")`     |
| `insert()`         | Inserts text at specific index | `sb.insert(5, " World")` |
| `replace()`        | Replaces characters            | `sb.replace(0, 5, "Hi")` |
| `delete()`         | Deletes characters             | `sb.delete(0, 5)`        |
| `deleteCharAt()`   | Deletes single character       | `sb.deleteCharAt(2)`     |
| `reverse()`        | Reverses string                | `sb.reverse()`           |
| `charAt()`         | Gets character at index        | `sb.charAt(1)`           |
| `setCharAt()`      | Changes character at index     | `sb.setCharAt(0, 'h')`   |
| `length()`         | Returns length                 | `sb.length()`            |
| `capacity()`       | Returns current capacity       | `sb.capacity()`          |
| `substring()`      | Extracts substring             | `sb.substring(0, 5)`     |
| `ensureCapacity()` | Increases capacity if needed   | `sb.ensureCapacity(50)`  |
| `toString()`       | Converts to String             | `sb.toString()`          |

public class StringBufferMethods {

    public static void main(String[] args) {

        StringBuffer sb = new StringBuffer("Hello");
        // append
        sb.append(" Java");
        System.out.println("append(): " + sb);
        // insert
        sb.insert(5, " World");
        System.out.println("insert(): " + sb);
        // replace
        sb.replace(0, 5, "Hi");
        System.out.println("replace(): " + sb);
        // delete
        sb.delete(2, 8);
        System.out.println("delete(): " + sb);
        // reverse
        sb.reverse();
        System.out.println("reverse(): " + sb);
        // length
        System.out.println("length(): " + sb.length());
        // capacity
        System.out.println("capacity(): " + sb.capacity());
    }
}

StringBuilder class:
-------------------
The StringBuilder class in Java is similar to StringBuffer in functionality but is not synchronized, 
making it more efficient for single-threaded environments. It is used when you need a mutable sequence of characters in a single-threaded application.
| Method             | Description                    | Example                  |
| ------------------ | ------------------------------ | ------------------------ |
| `append()`         | Adds text at end               | `sb.append(" Java")`     |
| `insert()`         | Inserts text at specific index | `sb.insert(5, " World")` |
| `replace()`        | Replaces characters            | `sb.replace(0, 5, "Hi")` |
| `delete()`         | Deletes characters             | `sb.delete(0, 5)`        |
| `deleteCharAt()`   | Deletes single character       | `sb.deleteCharAt(2)`     |
| `reverse()`        | Reverses string                | `sb.reverse()`           |
| `charAt()`         | Returns character at index     | `sb.charAt(1)`           |
| `setCharAt()`      | Changes character at index     | `sb.setCharAt(0, 'h')`   |
| `length()`         | Returns string length          | `sb.length()`            |
| `capacity()`       | Returns capacity               | `sb.capacity()`          |
| `substring()`      | Extracts substring             | `sb.substring(0, 5)`     |
| `ensureCapacity()` | Increases capacity             | `sb.ensureCapacity(50)`  |
| `toString()`       | Converts to String             | `sb.toString()`          |
| `indexOf()`        | Finds substring index          | `sb.indexOf("Java")`     |
| `lastIndexOf()`    | Finds last occurrence          | `sb.lastIndexOf("a")`    |

public class StringBuilderMethodsExample {

    public static void main(String[] args) {

        StringBuilder sb = new StringBuilder("Hello");
        // append
        sb.append(" Java");
        System.out.println("append(): " + sb);
        // insert
        sb.insert(5, " World");
        System.out.println("insert(): " + sb);
        // replace
        sb.replace(0, 5, "Hi");
        System.out.println("replace(): " + sb);
        // delete
        sb.delete(2, 8);
        System.out.println("delete(): " + sb);
        // reverse
        sb.reverse();
        System.out.println("reverse(): " + sb);
        // charAt
        System.out.println("charAt(2): " + sb.charAt(2));
        // setCharAt
        sb.setCharAt(0, 'A');
        System.out.println("setCharAt(): " + sb);
        // length
        System.out.println("length(): " + sb.length());
        // capacity
        System.out.println("capacity(): " + sb.capacity());
        // toString
        String str = sb.toString();
        System.out.println("toString(): " + str);
    }
}

| Feature       | String                      | StringBuffer                  |
| ------------- | --------------------------- | ----------------------------- |
| Mutability    | Immutable                   | Mutable                       |
| Modification  | Creates new object          | Modifies same object          |
| Performance   | Slower for frequent changes | Faster for frequent changes   |
| Memory Usage  | More memory usage           | Less memory usage             |
| Thread Safety | Not synchronized            | Synchronized and thread-safe  |
| Best Use Case | Fixed text                  | Frequent string modifications |

| Feature       | StringBuffer   | StringBuilder                |
| ------------- | -------------- | ---------------------------- |
| Mutable       | Yes            | Yes                          |
| Thread Safe   | Yes            | No                           |
| Synchronized  | Yes            | No                           |
| Performance   | Slower         | Faster                       |
| Best Use Case | Multithreading | Single-threaded applications |

Rules to Create an Immutable Class:
----------------------------------
| Rule                                  | Explanation                    |
| ------------------------------------- | ------------------------------ |
| `final class`                         | Prevents inheritance           |
| `final fields`                        | Field values cannot be changed |
| No setter methods                     | Prevents modification          |
| Initialize fields through constructor | Values set only once           |
| Provide only getters                  | Read-only access               |

public final class ImmutableClassExample {

    private final String name;
    private final int age;
    // Constructor
    public ImmutableClassExample(String name, int age) {
        this.name = name;
        this.age = age;
    }
    // Getter methods
    public String getName() {
        return name;
    }
    public int getAge() {
        return age;
    }
}

-Java Regex:
-----------
Java Regular Expressions, also known as Java Regex, are used to match patterns in strings. They are a powerful tool for text processing 
and pattern matching. Java provides built-in support for regular expressions through two main classes: Pattern and Matcher .
-The Matcher class is used to search for patterns defined by a Pattern object within a given input string. It provides methods to 
perform various operations on the matched patterns, such as finding all occurrences, replacing, and extracting groups.
| Method                                      | Description                 | Example                          |
| ------------------------------------------- | --------------------------- | -------------------------------- |
| `compile(String regex)`                     | Compiles regex into pattern | `Pattern.compile("\\d+")`        |
| `matcher(CharSequence input)`               | Creates matcher object      | `pattern.matcher("Java123")`     |
| `matches(String regex, CharSequence input)` | Checks complete match       | `Pattern.matches("\\d+", "123")` |
| `split(CharSequence input)`                 | Splits string using regex   | `pattern.split("a,b,c")`         |
| `quote(String s)`                           | Treats string literally     | `Pattern.quote(".")`             |

-The Pattern class is used to compile a regular expression into a pattern that can be used by the Matcher class for matching against input strings. 
It provides methods to create and manipulate regular expression patterns.
| Method                             | Description              | Example                     |
| ---------------------------------- | ------------------------ | --------------------------- |
| `find()`                           | Finds next match         | `matcher.find()`            |
| `matches()`                        | Checks full string match | `matcher.matches()`         |
| `group()`                          | Returns matched text     | `matcher.group()`           |
| `start()`                          | Returns start index      | `matcher.start()`           |
| `end()`                            | Returns end index        | `matcher.end()`             |
| `replaceAll(String replacement)`   | Replaces all matches     | `matcher.replaceAll("*")`   |
| `replaceFirst(String replacement)` | Replaces first match     | `matcher.replaceFirst("*")` |
| `group(int group)`                 | Returns capturing group  | `matcher.group(1)`          |
| `reset()`                          | Resets matcher           | `matcher.reset()`           |

Common Regex Symbols
| Regex | Meaning         |
| ----- | --------------- |
| `.`   | Any character   |
| `\\d` | Digit           |
| `\\D` | Non-digit       |
| `\\w` | Word character  |
| `\\s` | Whitespace      |
| `+`   | One or more     |
| `*`   | Zero or more    |
| `?`   | Optional        |
| `^`   | Start of string |
| `$`   | End of string   |

Common Regex Character Classes: used to match one character from a specific set of characters. They are written inside square brackets: [ ]
| Character Class | Meaning                                   | Example Match |
| --------------- | ----------------------------------------- | ------------- |
| `[abc]`         | Matches `a`, `b`, or `c`                  | `a`           |
| `[0-9]`         | Matches any digit from 0 to 9             | `5`           |
| `[a-z]`         | Matches lowercase letters                 | `m`           |
| `[A-Z]`         | Matches uppercase letters                 | `K`           |
| `[a-zA-Z]`      | Matches any uppercase or lowercase letter | `P`           |
| `[a-zA-Z0-9]`   | Matches letters and digits                | `A`, `5`      |
| `[^0-9]`        | Matches non-digit characters              | `A`, `#`      |
| `[^abc]`        | Matches characters except `a`, `b`, `c`   | `x`           |
| `[aeiou]`       | Matches vowels                            | `e`           |
| `[A-F]`         | Matches letters from A to F               | `C`           |


Common Flags in Pattern Class:
| Flag                       | Purpose                                 | Example                                             |
| -------------------------- | --------------------------------------- | --------------------------------------------------- |
| `Pattern.CASE_INSENSITIVE` | Ignores case while matching             | `Pattern.compile("java", Pattern.CASE_INSENSITIVE)` |
| `Pattern.MULTILINE`        | Enables multiline matching              | `Pattern.compile("^Java", Pattern.MULTILINE)`       |
| `Pattern.DOTALL`           | Makes `.` match newline characters also | `Pattern.compile("A.*B", Pattern.DOTALL)`           |
| `Pattern.UNICODE_CASE`     | Enables Unicode-aware case matching     | `Pattern.compile("ä", Pattern.UNICODE_CASE)`        |

Pattern Example:
| Pattern             | Meaning              |
| ------------------- | -------------------- |
| `"\\d+"`            | One or more digits   |
| `"[a-z]+"`          | Lowercase letters    |
| `"[A-Z]+"`          | Uppercase letters    |
| `"[0-9]{10}"`       | 10-digit number      |
| `"\\w+@\\w+\\.com"` | Simple email pattern |


import java.util.regex.*;

public class RegexExample {
    public static void main(String[] args) {
        String input =
                "The quick brown fox jumps over the lazy dog.";
        // Define regex pattern
        String regex = "quick.*fox";  //Matches any text that starts with "quick" and ends with "fox" with any characters in between.
        // Compile pattern
        Pattern pattern = Pattern.compile(regex);
        // Create matcher object
        Matcher matcher = pattern.matcher(input);
        // Perform matching
        if (matcher.find()) {
            System.out.println(
                    "Pattern found: " + matcher.group());
        } else {
            System.out.println("Pattern not found.");
        }
    }
}

import java.util.regex.*;

public class CharacterClassExample {
    public static void main(String[] args) {
        String input ="The car costs $500, the bike costs $200.";
        // Regex pattern to find digits
        String regex = "[0-9]+";    //Matches a sequence of one or more digits (0–9)
        // Compile regex
        Pattern pattern = Pattern.compile(regex);
        // Create matcher object
        Matcher matcher = pattern.matcher(input);
        // Find and print all matches
        while (matcher.find()) {
            System.out.println("Found: " + matcher.group());
        }
    }
}

Common Regex Quantifiers:
| Quantifier | Meaning                   | Example  | Description                         | Real Example                |
| ---------- | ------------------------- | -------- | ----------------------------------- | --------------------------- |
| `*`        | Zero or more times        | `a*`     | Matches zero or more `a` characters | `"caaar"` → matches `aaa`   |
| `+`        | One or more times         | `a+`     | Matches one or more `a` characters  | `"cat"` → matches `a`       |
| `?`        | Zero or one time          | `a?`     | Matches optional `a`                | `"color"` and `"colour"`    |
| `{n}`      | Exactly `n` times         | `a{3}`   | Matches exactly 3 `a` characters    | `"baaat"` → matches `aaa`   |
| `{n,}`     | At least `n` times        | `a{2,}`  | Matches 2 or more `a` characters    | `"caaaat"` → matches `aaaa` |
| `{n,m}`    | Between `n` and `m` times | `a{2,4}` | Matches 2 to 4 `a` characters       | `"caaaat"` → matches `aaaa` |

import java.util.regex.*;
public class QuantifiersExample {
    public static void main(String[] args) {
        String input = "abbbbbbcde";
        // Regex to match b occurring 2 to 5 times
        String regex = "b{2,5}";
        // Compile regex
        Pattern pattern = Pattern.compile(regex);
        // Create matcher
        Matcher matcher = pattern.matcher(input);
        // Find matches
        while (matcher.find()) {
            System.out.println(
                    "Found: " + matcher.group());
        }
    }
}

Common Regex Metacharacters:
| Metacharacter | Meaning                             | Example    | Real Example         |
| ------------- | ----------------------------------- | ---------- | -------------------- |
| `.`           | Any single character except newline | `a.b`      | `"acb"`, `"a1b"`     |
| `^`           | Start of line/string                | `^Java`    | `"Java Programming"` |
| `$`           | End of line/string                  | `Java$`    | `"I love Java"`      |
| `\|`          | OR operator                         | `cat\|dog` | `"dog is pet"`       |

import java.util.regex.*;

public class MetacharactersExample {

    public static void main(String[] args) {
        String input = "The cat sat on the mat.";
        // Regex to find words starting with c or m
        String regex = "\\b(c|m)\\w+\\b";
        // Compile regex
        Pattern pattern = Pattern.compile(regex);
        // Create matcher
        Matcher matcher = pattern.matcher(input);
        // Find matches
        while (matcher.find()) {
            System.out.println(
                    "Found: " + matcher.group());
        }
    }
}
"\\b(c|m)\\w+\\b" → Matches complete words that start with either 'c' or 'm' followed by one or more word characters.
\\b   → Word boundary
(c|m) → c OR m
\\w+  → One or more word characters
\\b   → End word boundary

-Exception Handling in Java:
---------------------------
It is a mechanism in Java that deals with runtime errors or exceptional situations that may occur during the program's execution. 
When an exception occurs, it disrupts the normal flow of the program, and without proper handling, it can lead to program termination.
Java Try-Catch Block:
--------------------
If an exception occurs within the try block, it is caught by the corresponding catch block, which contains code to handle the exception.

Java Multiple Catch Block:
-------------------------
Java allows us to use multiple catch blocks to handle different types of exceptions that may arise within the try block.
public class MultipleCatchExample {
    public static void main(String[] args) {
        try {
            String str = null;
            // Throws NullPointerException
            System.out.println(str.length());
        } catch (NullPointerException e) {
            System.out.println(
                    "NullPointerException caught: "
                    + e.getMessage());
        } catch (ArithmeticException e) {
            System.out.println(
                    "ArithmeticException caught: "
                    + e.getMessage());
        }
        System.out.println(
                "Program continues after exception handling.");
    }
}

Java Nested Try:
----------------
We can use nested try blocks to handle exceptions at different levels of code execution.
public class NestedTryExample {
    public static void main(String[] args) {
        try {

            int[] arr = {1, 2, 3};
            int num1 = 10;
            int num2 = 0;
            try {
                // Throws ArithmeticException
                int result = num1 / num2;
                System.out.println("Result: " + result);
            } catch (ArithmeticException e) {
                System.out.println(
                        "Inner Exception caught: "
                        + e.getMessage());
            }
            // Throws ArrayIndexOutOfBoundsException
            System.out.println(arr[5]);
        } catch (ArrayIndexOutOfBoundsException e) {
            System.out.println(
                    "Outer Exception caught: "
                    + e.getMessage());
        }
        System.out.println(
                "Program continues after exception handling.");
    }
}

Java Finally Block:
-------------------
The finally block is used to specify code that should be executed regardless of whether an exception occurs or not. It is often used to release resources, 
close files or clean up operations.

public class FinallyExample {
    public static void main(String[] args) {
        BufferedReader br = null;
        try {
            br = new BufferedReader(
                    new FileReader("input.txt"));
            String line;
            while ((line = br.readLine()) != null) {
                System.out.println(line);
            }
        } catch (FileNotFoundException e) {
            System.out.println(
                    "File not found: " + e.getMessage());
        } catch (IOException e) {
            System.out.println(
                    "IOException: " + e.getMessage());
        } finally {
            try {
                if (br != null)
                    br.close(); // Close resource    //ensure that it gets closed regardless of whether an exception occurs or not.
            } catch (IOException e) {
                System.out.println(
                        "Error closing BufferedReader: "
                        + e.getMessage());
            }
        }
    }
}

Java Throw Keyword:
------------------
The throw keyword is used to explicitly throw an exception. It is often used when the program encounters an exceptional situation that 
cannot be handled by the program.
public class ThrowExample {
    public static void main(String[] args) {
        int age = 15;
        if (age < 18) {
            throw new ArithmeticException(
                    "Age must be greater than or equal to 18.");
        }
        System.out.println("You are eligible to vote.");
    }
}

Java Exception Propagation:
--------------------------
Java allows exceptions to be propagated from a method to its caller method. If a method does not handle an exception, 
it can declare the exception using the throws keyword, and the caller method can handle it.
In below example, the method2() throws a NullPointerException , which is propagated to method1() , and finally caught in the ` main()` method.
public class ExceptionPropagationExample {
    public static void main(String[] args) {
        try {
            method1();
        } catch (NullPointerException e) {
            System.out.println("Exception caught: "+ e.getMessage());
        }
    }

    public static void method1() throws NullPointerException {
        method2();
    }

    public static void method2() throws NullPointerException {
        String str = null;
        System.out.println(str.length());
    }
}
//throws → Used in a method signature to declare that the method may throw one or more exceptions, allowing the caller to handle or propagate them.

| Term         | Type    | Purpose                                                                                                       | Example                                   |
| ------------ | ------- | ------------------------------------------------------------------------------------------------------------- | ----------------------------------------- |
| `throw`      | Keyword | Used to explicitly throw an exception within a method.                                                        | `throw new ArithmeticException("Error");` |
| `throws`     | Keyword | Used in a method signature to declare that the method may throw one or more exceptions.                       | `void test() throws IOException { }`      |
| `try`        | Block   | Contains code that may generate an exception.                                                                 | `try { riskyCode(); }`                    |
| `catch`      | Block   | Handles an exception thrown by the `try` block.                                                               | `catch(Exception e) { }`                  |
| `finally`    | Block   | Executes whether an exception occurs or not.                                                                  | `finally { cleanup(); }`                  |
| `final`      | Keyword | Used to declare a constant variable, a method that cannot be overridden, or a class that cannot be inherited. | `final int AGE = 18;`                     |
| `finalize()` | Method  | Called by the garbage collector before reclaiming an object's memory. *(Deprecated since Java 9)*             | `protected void finalize() { }`           |

Exception Handling with Method Overriding:
-----------------------------------------
When a subclass overrides a method from its superclass, it can choose to throw the same exception as the superclass method or a subclass of that exception.
class Parent {
    void method() throws Exception {
        System.out.println("Parent's method");
    }
}

class Child extends Parent {
    @Override
    void method() throws ArithmeticException {
        System.out.println("Child's method");
    }
}

public class ExceptionHandlingWithMethodOverriding {
    public static void main(String[] args) {
        Parent parent = new Child();  //When the method is called through the reference of the Parent class but pointing to a Child object, 
		                              //the Child class's method is executed.
        try {
            parent.method();
        } catch (ArithmeticException e) {
            System.out.println(
                    "Exception caught: "
                    + e.getMessage());
        }
    }
}

Java Custom Exceptions:
----------------------
you can create custom exceptions by extending the built-in Exception or RuntimeException classes. Custom exceptions allow you to handle specific
exceptional situations in your application and provide meaningful error messages for better debugging and error reporting.
// Custom exception class
class CustomException extends Exception {
    public CustomException(String message) {
        super(message);  //The CustomException class has a constructor that takes a message as an argument and passes it to the super() constructor of the Exception class.

    }
}

// Class with a method that throws the custom exception
class ExampleClass {
    public void performOperation(int num)
            throws CustomException {
        if (num < 0) {
            throw new CustomException(
                    "Number cannot be negative.");
        }
        System.out.println("Operation successful.");
    }
}

// Main class
public class CustomExceptionExample {
    public static void main(String[] args) {
        ExampleClass example = new ExampleClass();
        try {
            example.performOperation(-5);
            example.performOperation(10);
        } catch (CustomException e) {
            System.out.println(
                    "Exception caught: "
                    + e.getMessage());
        }
    }
}

-Java Inner Classes:
------------------
In Java, an inner class is a class defined within another class. Inner classes provide a way to logically group classes that belong together, making the code more
readable and maintainable. There are several types of inner classes in Java:
Member Inner Class:
------------------
A Member Inner class is defined as a non-static class inside another class. It has access to all members of the enclosing class, including private members. 
To create an instance of a Member Inner class, it must be associated with an instance of the enclosing class.
| Feature                                | Description |
| -------------------------------------- | ----------- |
| Defined inside another class           | Yes         |
| Non-static                             | Yes         |
| Can access outer class private members | Yes         |
| Requires outer class object            | Yes         |

class Outer {
    private int outerField = 10;
    // Member Inner Class
    class Inner {
        private int innerField = 20;
        void display() {
            System.out.println(
                    "Outer Field: " + outerField);
            System.out.println(
                    "Inner Field: " + innerField);
        }
    }
}

public class MemberInnerClassExample {
    public static void main(String[] args) {
        Outer outerObj = new Outer();
        Outer.Inner innerObj =
                outerObj.new Inner();
        innerObj.display();
    }
}

Anonymous Inner Class:
---------------------
An Anonymous Inner class is a type of inner class that does not have a name. It is declared and instantiated at the same time. It is used when you need to create a
class instance only once.
interface Greeting {
    void greet();
}

public class AnonymousInnerClassExample {
    public static void main(String[] args) {
        Greeting greeting = new Greeting() {             //implementation of interface
            @Override
            public void greet() {
                System.out.println(
                        "Hello, Anonymous Inner Class!");
            }
        };
        greeting.greet();
    }
}

Local Inner Class:
-----------------
A Local Inner class is defined inside a block of code, typically within a method. It is only accessible within the block where it is declared.
public class LocalInnerClassExample {
    private int outerField = 100;
    void display() {
        // Local Inner Class
        class Inner {
            void innerMethod() {
                System.out.println(
                        "Outer Field from Inner: "
                        + outerField);
            }
        }
        Inner innerObj = new Inner();
        innerObj.innerMethod();
    }

    public static void main(String[] args) {
        LocalInnerClassExample outerObj =
                new LocalInnerClassExample();
        outerObj.display();
    }
}

Static Nested Class:
--------------------
A Static Nested class is a static class defined inside another class. It is similar to a regular class but is just nested for packaging convenience. 
A static nested class does not have access to the non-static members of the enclosing class.
class Outer {
    private static int outerStaticField = 50;
    // Static Nested Class
    static class StaticNested {
        void display() {
            System.out.println(
                    "Outer Static Field from Static Nested: "
                    + outerStaticField);
        }
    }
}

public class StaticNestedClassExample {
    public static void main(String[] args) {
        Outer.StaticNested nestedObj =
                new Outer.StaticNested();
        nestedObj.display();
    }
}

Nested Interface:
----------------
A Nested Interface is an interface declared within another class or interface. It is implicitly static and accessible within the enclosing class or interface. 
It is used for logical grouping of related interfaces.
class Outer {
    interface NestedInterface {
        void display();
    }
}

public class NestedInterfaceExample {
    public static void main(String[] args) {
        Outer.NestedInterface nestedObj =
                new Outer.NestedInterface() {
            @Override
            public void display() {
                System.out.println(
                        "Hello, Nested Interface!");
            }
        };
        nestedObj.display();
    }
}

-Java Multithreading:
--------------------
It is a programming concept that allows multiple threads to execute independently within a single Java program. It enables efficient utilization of CPU resources 
and helps in building responsive and concurrent applications.
The life cycle of a thread in Java consists of the following states:
| State                 | Description                                                                                |
| --------------------- | ------------------------------------------------------------------------------------------ |
| **New**               | The thread is created but has not yet been started using `start()`.                        |
| **Runnable**          | The thread is ready to run and waiting for CPU time from the scheduler.                    |
| **Running**           | The thread is currently executing its `run()` method.                                      |
| **Blocked / Waiting** | The thread is waiting for a lock, another thread, or a specific condition to be satisfied. |
| **Terminated (Dead)** | The thread has completed execution or has been stopped due to an exception.                |

| Method           | Resulting State               |
| ---------------- | ----------------------------- |
| `start()`        | New → Runnable                |
| `sleep()`        | Running → Timed Waiting       |
| `wait()`         | Running → Waiting             |
| `join()`         | Calling thread enters Waiting |
| Lock unavailable | Running → Blocked             |
| Task completed   | Running → Terminated          |


+------+    start()    +----------+   CPU assigned   +---------+
| New  | ----------->  | Runnable | --------------> | Running |
+------+               +----------+                  +---------+
                             ^                            |
                             |                            |
                             |                            | waiting for
                             |                            | lock/resource
                             |                            v
                       resource available        +------------------+
                             |                   | Blocked/Waiting  |
                             +-------------------+------------------+
                                                 |
                                                 | task completed
                                                 v
                                          +------------+
                                          | Terminated |
                                          +------------+
How to Create a Thread:
----------------------
1. By extending the Thread class: 
class MyThread extends Thread {
    @Override
    public void run() {
        // Code executed by the thread
        System.out.println("Thread is running...");
    }
}

public class ThreadExample {
    public static void main(String[] args) {
        MyThread thread = new MyThread();
        thread.start(); // Starts a new thread
    }
}

2.By implementing the Runnable interface:
class MyRunnable implements Runnable {
    @Override
    public void run() {
        // Code executed by the thread
        System.out.println("Thread is running...");
    }
}

public class RunnableExample {
    public static void main(String[] args) {
        Thread thread =
                new Thread(new MyRunnable());
        thread.start(); // Starts the thread
    }
}

>Thread Scheduler: The thread scheduler is responsible for assigning CPU time to threads based on their priorities and states.
>Sleeping a Thread: The sleep() method is used to make a thread pause its execution for a specified amount of time.
try {
    Thread.sleep(1000); // Sleep for 1 second
} catch (InterruptedException e) {
    e.printStackTrace();
}


Important Notes About Threads:
| Topic                        | Description                                                                                                                                  |
| ---------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------- |
| **Starting a Thread Twice**  | A thread can be started only once. If you call `start()` again on the same thread object, Java throws `IllegalThreadStateException`.         |
| **Calling `run()` Directly** | Never call `run()` directly to start a thread. It executes in the current thread like a normal method call and does not create a new thread. |
| **Using `start()`**          | Always use `start()` to create a new thread. The JVM then invokes `run()` in a separate thread.                                              |
Thread t = new Thread();
Ex:
t.start(); // Valid
t.start(); // Throws IllegalThreadStateException

MyThread t = new MyThread();

t.run();   // Normal method call, no new thread
t.start(); // Creates a new thread and calls run()

| Concept         | Description                                                                 | Example                                    |
| --------------- | --------------------------------------------------------------------------- | ------------------------------------------ |
| `join()`        | Makes the current thread wait until another thread completes its execution. | `thread.join();`                           |
| `setName()`     | Assigns a name to a thread for easier identification and debugging.         | `thread.setName("MyThread");`              |
| `setPriority()` | Sets the priority of a thread to influence CPU scheduling.                  | `thread.setPriority(Thread.MAX_PRIORITY);` |

Thread thread = new Thread(() -> {
// Thread task
});
thread.setName("MyThread")

Daemon Thread: A daemon thread is a thread that runs in the background and does not prevent the JVM from exiting when the main thread finishes. You can
set a thread as a daemon thread using setDaemon(true) .
| User Thread                  | Daemon Thread                                      |
| ---------------------------- | -------------------------------------------------- |
| Keeps JVM running            | Does not keep JVM running                          |
| Main thread is a user thread | Runs in background                                 |
| JVM waits for completion     | JVM may terminate it anytime when user threads end |

// Creates a thread using a lambda expression
Thread daemonThread = new Thread(() -> {
    // Daemon thread task
});
// Marks the thread as a daemon (background) thread
daemonThread.setDaemon(true);
// Starts the daemon thread
daemonThread.start();

| Concept           | Description                                                                                                                  | Example                                                       |
| ----------------- | ---------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------- |
| **Thread Pool**   | A collection of reusable threads managed by an executor to avoid the overhead of creating and destroying threads repeatedly. | `ExecutorService executor = Executors.newFixedThreadPool(5);` |
| **Thread Group**  | A mechanism for grouping multiple threads together for management purposes. Rarely used in modern Java applications.         | `ThreadGroup group = new ThreadGroup("MyGroup");`             |
| **Shutdown Hook** | A thread that executes automatically when the JVM is shutting down, typically used for cleanup tasks.                        | `Runtime.getRuntime().addShutdownHook(new Thread(() -> {}));` |
Ex: Thread Pool
// Creates a thread pool with 5 worker threads
ExecutorService executor =
        Executors.newFixedThreadPool(5);
// Submits a task to the thread pool
executor.submit(() ->
        System.out.println("Task executed"));
// Shuts down the thread pool
executor.shutdown();

Ex: Shutdown Hook
// Registers a shutdown hook
Runtime.getRuntime().addShutdownHook(
        new Thread(() -> {
            System.out.println(
                    "Cleanup before JVM shutdown");
        }));
		
| Concept                       | Description                                                                                               | Example                                           |
| ----------------------------- | --------------------------------------------------------------------------------------------------------- | ------------------------------------------------- |
| **Performing Multiple Tasks** | Multiple threads can execute different tasks concurrently, improving application performance.             | `executor.submit(task1); executor.submit(task2);` |
| **Garbage Collection (GC)**   | JVM automatically reclaims memory occupied by objects that are no longer reachable.                       | `System.gc();` *(request only)*                   |
| **Runtime Class**             | Provides access to the Java Runtime Environment and allows interaction with the JVM and operating system. | `Runtime.getRuntime();`                           |

Ex: Multiple taks
// Creates a thread pool with 2 threads
ExecutorService executor =
        Executors.newFixedThreadPool(2);
// Task 1
executor.submit(() ->
        System.out.println("Task 1"));
// Task 2
executor.submit(() ->
        System.out.println("Task 2"));
executor.shutdown();

Runtime class:
| Method                  | Description                          |
| ----------------------- | ------------------------------------ |
| `getRuntime()`          | Returns the Runtime instance         |
| `availableProcessors()` | Returns number of CPU cores          |
| `freeMemory()`          | Returns available JVM memory         |
| `totalMemory()`         | Returns total JVM memory             |
| `maxMemory()`           | Returns maximum JVM memory           |
| `gc()`                  | Requests garbage collection          |
| `exec(String command)`  | Executes an operating system command |

-Java Synchronization:
---------------------
Synchronization is a technique used to control access to shared resources in a multi-threaded environment. When multiple threads try to access shared resources concurrently, 
synchronization ensures that only one thread can access the critical section (the synchronized block) at a time. This prevents race conditions and maintains data consistency.
Synchronized Block:
------------------ 
use the synchronized keyword to create a synchronized block. A synchronized block allows you to specify a block of code that should be executed by only one thread at a time. 
The block is synchronized on a specified object, ensuring that only one thread at a time can enter that block for that particular object.
class BankAccount {
    private int balance = 1000;
    public synchronized void withdraw(int amount) {
        balance -= amount;
    }
    public int getBalance() {
        return balance;
    }
}

class SharedResource {
    public synchronized void instanceMethod() {}
    public static synchronized void staticMethod() {}
}

| Method           | Lock                 |
| ---------------- | -------------------- |
| instanceMethod() | this                 |
| staticMethod()   | SharedResource.class |


| Feature                     | Instance Synchronized | Static Synchronized |
| --------------------------- | --------------------- | ------------------- |
| Lock Object                 | `this`                | `ClassName.class`   |
| Scope                       | Per object            | Entire class        |
| Multiple Objects Allowed?   | Yes                   | No                  |
| Lock Shared Across Objects? | No                    | Yes                 |

class SharedResource {
    public static synchronized void staticSyncMethod() {
        // Static synchronized method can be accessed by only one thread at a time.
        // Critical section code
    }
    public static void staticSyncBlock() {
        synchronized (SharedResource.class) {
            // Static synchronized block can be accessed by only one thread at a time.
            // Critical section code
        }
    }
}
Note: Both of these acquire the same lock on SharedResource.class ie use the same class-level lock.
      SharedResource obj1 = new SharedResource();
      SharedResource obj2 = new SharedResource();
      //Instance Synchronization
      obj1.instanceMethod();
      obj2.instanceMethod();
      ✅ Can execute concurrently because: obj1 lock != obj2 lock
	  
	  obj1.staticMethod();
      obj2.staticMethod();
      ❌ Cannot execute concurrently because:
	  SharedResource.class lock == SharedResource.class lock
	  
Deadlock in Java: 
----------------
Deadlock occurs when two or more threads are unable to proceed because each of them is waiting for a resource that is held by another thread. It leads to a circular waiting scenario, 
causing the threads to get stuck indefinitely. Deadlocks are undesirable and can lead to application hang-ups
class SharedResource {
    public synchronized void method1(SharedResource resource) {
        // This method acquires a lock on this object
        // (current instance of SharedResource).
        // It will now try to call method2 on the 'resource' object.
        resource.method2(this);
    }
    public synchronized void method2(SharedResource resource) {
        // This method acquires a lock on this object
        // (current instance of SharedResource).
        // It will now try to call method1 on the 'resource' object.
        resource.method1(this);
    }
}

SharedResource resource1 = new SharedResource();
SharedResource resource2 = new SharedResource();

Thread t1 = new Thread(() -> {
    resource1.method1(resource2);
});

Thread t2 = new Thread(() -> {
    resource2.method2(resource1);
});

Thread-1
Holding: resource1
Waiting: resource2

Thread-2
Holding: resource2
Waiting: resource1

| Prevention Technique                | Details                                                                                                                                               |
| ----------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------|
| **Acquire Locks in the Same Order** | Acquire multiple locks in a fixed order to prevent circular waiting.Use in Applications with multiple synchronized resources.                         |
| **Use a Single Lock**               | Use one common lock for all critical sections to avoid lock dependency.Use in  Simple applications with shared resources.                             |
| **Use `ReentrantLock.tryLock()`**   | Attempts to acquire a lock without waiting forever if it is unavailable.Use in High-concurrency applications requiring non-blocking lock acquisition. |

Inter-thread Communication:
--------------------------
Inter-thread communication allows threads to communicate with each other by waiting and notifying on a shared object. This is achieved using the wait() , notify() , and notifyAll() methods 
from the Object class. Threads can wait for certain conditions to be satisfied before proceeding.
| Method            | Explanation                                                                              | Most Common Use                                                                      |
| ----------------- | ---------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------ |
| **`wait()`**      | Causes the current thread to release the lock and wait until another thread notifies it. | Producer-Consumer scenarios where a thread must wait for a condition to become true. |
| **`notify()`**    | Wakes up one waiting thread that is waiting on the same object.                          | When only one waiting thread needs to continue execution.                            |
| **`notifyAll()`** | Wakes up all threads waiting on the same object.                                         | Multiple producer and consumer threads waiting for resources.                        |

| Method        | Must Be Called Inside Synchronized Block/Method? | Releases Lock? |
| ------------- | ------------------------------------------------ | -------------- |
| `wait()`      | ✅ Yes                                            | ✅ Yes          |
| `notify()`    | ✅ Yes                                            | ❌ No           |
| `notifyAll()` | ✅ Yes                                            | ❌ No           |

class SharedResource {
    public synchronized void produce() {
        while (queueIsFull()) {
            try {
                // Wait until the queue is not full
                wait();
            } catch (InterruptedException e) {
                e.printStackTrace();
            }
        }
        // Produce an item and add it to the queue
        addToQueue();
        // Notify waiting consumer threads that a new item is available in the queue
        notifyAll();
    }

    public synchronized void consume() {
        while (queueIsEmpty()) {
            try {
                // Wait until the queue is not empty
                wait();
            } catch (InterruptedException e) {
                e.printStackTrace();
            }
        }
        // Consume an item from the queue
        removeFromQueue();
        // Notify waiting producer threads that space is available in the queue
        notifyAll();
    }
}

Interrupting Thread:
-------------------
The interrupt() method is used to interrupt a thread in Java. When a thread is interrupted, it sets the thread's interrupt status to true. A thread can check its interrupt status using 
isInterrupted() method or by catching InterruptedException
| Method                 | Clears Interrupt Flag? |
| ---------------------- | ---------------------- |
| `isInterrupted()`      | ❌ No                   |
| `Thread.interrupted()` | ✅ Yes                  |

Thread myThread = new Thread(() -> {
    while (!Thread.currentThread().isInterrupted()) {
        // Thread's task
    }
});
myThread.start();
// Somewhere else in the code, you can interrupt the thread:
myThread.interrupt();

Reentrant Monitor: 
-----------------
Java's synchronization is based on a concept called a "reentrant monitor." It means that a thread can acquire the lock on an object multiple times without being blocked by itself. 
Reentrant synchronization ensures that a thread can re-enter a synchronized block that it already holds the lock for, without deadlocking itself.
| Feature                      | Explanation                                                                      | Most Common Use                                            |
| ---------------------------- | -------------------------------------------------------------------------------- | ---------------------------------------------------------- |
| **Reentrant Monitor**        | Allows a thread to acquire the same lock multiple times without blocking itself. | Nested synchronized method calls within the same object.   |
| **Lock Hold Count**          | Java keeps track of how many times a thread has acquired a lock.                 | Managing recursive or nested synchronized operations.      |
| **Self-Deadlock Prevention** | Prevents a thread from waiting on a lock it already owns.                        | Object-oriented designs with synchronized method chaining. |
class SharedResource {
    public synchronized void method1() {
        System.out.println("Inside method1");
        method2();
    }
    public synchronized void method2() {
        System.out.println("Inside method2");
    }
}

public class Main {
    public static void main(String[] args) {
        SharedResource resource = new SharedResource();
        resource.method1();
    }
}
Main Thread → method1() → Lock Acquired → method2() → Re-enters Lock → Execution Complete → Lock Released

-Java I/OInput/Output):Java I/O is a framework that enables reading data from input sources and writing data to output destinations using streams, files, and object serialization mechanisms
----------------------
1. FileInputStream and FileOutputStream: FileInputStream and FileOutputStream classes are used for reading from and writing to files, respectively
---------------------------------------
import java.io.FileInputStream;   // Imports FileInputStream for reading data from a file
import java.io.FileOutputStream;  // Imports FileOutputStream for writing data to a file
import java.io.IOException;       // Imports IOException for handling file-related exceptions
public class FileStreamExample {
    public static void main(String[] args) {
        String content = "Hello, this is some content to write to a file!"; // Data to be written
        // Writing data to the file
        try (FileOutputStream fos = new FileOutputStream("output.txt")) {
            byte[] data = content.getBytes(); // Converts String to bytes
            fos.write(data); // Writes bytes to the file
            System.out.println("Content written to the file successfully!");
        } catch (IOException e) {
            e.printStackTrace();
        }
        // Reading data from the file
        try (FileInputStream fis = new FileInputStream("output.txt")) {
            int data;
            System.out.println("\nReading content from file:");
            while ((data = fis.read()) != -1) { // Reads one byte at a time, It returns an int value between 0 and 255. When the end of the file is reached, it returns -1.
                System.out.print((char) data);  // Converts byte to character and prints
            }
        } catch (IOException e) {
            e.printStackTrace();
        }
    }
}
Create Content → Open output.txt → Convert String to Bytes → Write Bytes to File → Close FileOutputStream → Display Success Message

Note: We also can do for reading for small files
      byte[] data = fis.readAllBytes();
      System.out.println(new String(data));
	  or
	  Scanner scanner = new Scanner(new File("output.txt"));
      while (scanner.hasNextLine()) {
          System.out.println(scanner.nextLine());
      }
      scanner.close();
	  
| Approach              | Best For                                      |
| --------------------- | --------------------------------------------- |
| `fis.read()`          | Learning and small examples                   |
| `fis.read(byte[])`    | General file reading                          |
| `readAllBytes()`      | Small files                                   |
| `BufferedInputStream` | Large files and performance                   |
| `Scanner`             | Simple text processing                        |
| `BufferedReader`      | Reading text files line by line (most common) |

2. BufferedOutputStream and BufferedInputStream: These classes provide buffering capabilities to enhance the performance of I/O operations by reducing the number of direct system calls.
-----------------------------------------------
BufferedInputStream and BufferedOutputStream wrap file streams with an internal buffer, reducing disk access and significantly improving I/O performance.
| Concept                 | Explanation                                                                                          | Most Common Use                                   |
| ----------------------- | ---------------------------------------------------------------------------------------------------- | ------------------------------------------------- |
| **BufferedInputStream** | Reads data using an internal buffer instead of directly accessing the file for every read operation. | Faster file reading and improved I/O performance. |
| **Buffer**              | Temporary memory used to store chunks of data.                                                       | Reducing expensive disk access operations.        |

| Class                    | Explanation                                                      | Most Common Use                 |
| ------------------------ | ---------------------------------------------------------------- | ------------------------------- |
| **BufferedOutputStream** | Writes data to a buffer before writing to the file.              | Faster file writing operations. |
| **BufferedInputStream**  | Reads data into a buffer before providing it to the application. | Faster file reading operations. |

import java.io.BufferedInputStream;   // Imports BufferedInputStream for buffered reading
import java.io.BufferedOutputStream;  // Imports BufferedOutputStream for buffered writing
import java.io.FileInputStream;       // Imports FileInputStream for reading from a file
import java.io.FileOutputStream;      // Imports FileOutputStream for writing to a file
import java.io.IOException;           // Imports IOException for exception handling
public class BufferedStreamExample {
    public static void main(String[] args) {
        String content = "Hello, this is Buffered Stream Example!";
        // Writing data using BufferedOutputStream
        try (BufferedOutputStream bos =
                 new BufferedOutputStream(new FileOutputStream("output.txt"))) {
            byte[] data = content.getBytes(); // Convert String to byte array
            bos.write(data); // Write data to buffer
            System.out.println("Content written successfully!");
        } catch (IOException e) {
            e.printStackTrace();
        }

        // Reading data using BufferedInputStream
        try (BufferedInputStream bis =
                 new BufferedInputStream(new FileInputStream("output.txt"))) {
            int data;
            System.out.println("\nReading content from file:");
            // Read one byte at a time until end of file
            while ((data = bis.read()) != -1) {
                // Convert byte to character and print
                System.out.print((char) data);
            }

        } catch (IOException e) {
            e.printStackTrace();
        }
    }
}

3. SequenceInputStream:
----------------------
| Class                   | Explanation                                                                       | Most Common Use                                            |
| ----------------------- | --------------------------------------------------------------------------------- | ---------------------------------------------------------- |
| **SequenceInputStream** | Combines multiple input streams into a single stream and reads them sequentially. | Reading data from multiple files as if they were one file. |

import java.io.FileInputStream;     // Imports FileInputStream for reading files
import java.io.SequenceInputStream; // Imports SequenceInputStream for combining streams
import java.io.IOException;         // Imports IOException for exception handling
public class SequenceInputStreamExample {
    public static void main(String[] args) {
        // Opens file1.txt and file2.txt for reading
        try (FileInputStream fis1 = new FileInputStream("file1.txt");
             FileInputStream fis2 = new FileInputStream("file2.txt");
             // Combines both input streams into a single stream
             SequenceInputStream sis = new SequenceInputStream(fis1, fis2)) {
            int data;
            // Reads data from the combined stream until EOF
            while ((data = sis.read()) != -1) {
                // Converts byte to character and prints
                System.out.print((char) data);
            }

        } catch (IOException e) {
            // Prints exception details
            e.printStackTrace();
        }
    }
}

4. ByteArrayOutputStream and ByteArrayInputStream: used for reading from and writing to byte arrays, respectively.
-------------------------------------------------
| Class                     | Explanation                             | Most Common Use                                       |
| ------------------------- | --------------------------------------- | ----------------------------------------------------- |
| **ByteArrayOutputStream** | Writes data to a byte array in memory.  | Creating byte arrays dynamically without using files. |
| **ByteArrayInputStream**  | Reads data from a byte array in memory. | Processing byte array data as an input stream.        |

import java.io.ByteArrayOutputStream; // Imports ByteArrayOutputStream for writing to a byte array
import java.io.ByteArrayInputStream;  // Imports ByteArrayInputStream for reading from a byte array
import java.io.IOException;           // Imports IOException for exception handling
public class ByteArrayStreamExample {
    public static void main(String[] args) {
        try (ByteArrayOutputStream baos = new ByteArrayOutputStream()) {
            String content = "Hello, this is some content to write to a byte array!";
            byte[] data = content.getBytes(); // Converts String to byte array
            baos.write(data); // Writes data to the byte array
            System.out.println("Content written to the byte array successfully!");
            // Gets the byte array from ByteArrayOutputStream
            byte[] byteArray = baos.toByteArray();
            // Reads data from the byte array
            try (ByteArrayInputStream bais = new ByteArrayInputStream(byteArray)) {
                int ch;
                System.out.println("\nReading content from byte array:");
                while ((ch = bais.read()) != -1) {
                    System.out.print((char) ch); // Converts byte to character and prints
                }
            }

        } catch (IOException e) {
            e.printStackTrace();
        }
    }
}

5. DataOutputStream and DataInputStream: provide methods to write and read primitive data types and strings to and from streams, respectively
---------------------------------------
| Class                | Explanation                                                    | Most Common Use                        |
| -------------------- | -------------------------------------------------------------- | -------------------------------------- |
| **DataOutputStream** | Writes Java primitive data types and strings in binary format. | Storing structured data in files.      |
| **DataInputStream**  | Reads Java primitive data types and strings in binary format.  | Retrieving structured data from files. |

| Write Method     | Read Method     |
| ---------------- | --------------- |
| `writeInt()`     | `readInt()`     |
| `writeDouble()`  | `readDouble()`  |
| `writeBoolean()` | `readBoolean()` |
| `writeChar()`    | `readChar()`    |
| `writeFloat()`   | `readFloat()`   |
| `writeLong()`    | `readLong()`    |
| `writeUTF()`     | `readUTF()`     |

import java.io.DataInputStream;    // Imports DataInputStream for reading primitive data types
import java.io.DataOutputStream;   // Imports DataOutputStream for writing primitive data types
import java.io.FileInputStream;    // Imports FileInputStream for reading from a file
import java.io.FileOutputStream;   // Imports FileOutputStream for writing to a file
import java.io.IOException;        // Imports IOException for exception handling
public class DataOutputStreamExample {
    public static void main(String[] args) {
        // Writing primitive data types to a file
        try (DataOutputStream dos =
                 new DataOutputStream(new FileOutputStream("data.txt"))) {
            dos.writeInt(42); // Writes an integer
            dos.writeDouble(3.14); // Writes a double
            dos.writeUTF("Hello, this is a string."); // Writes a String
            System.out.println("Data written to the file successfully!");
        } catch (IOException e) {
            e.printStackTrace();
        }
        // Reading primitive data types from the file
        try (DataInputStream dis =
                 new DataInputStream(new FileInputStream("data.txt"))) {
            int intValue = dis.readInt(); // Reads an integer
            double doubleValue = dis.readDouble(); // Reads a double
            String stringValue = dis.readUTF(); // Reads a String
            System.out.println("Read values: "
                    + intValue + ", "
                    + doubleValue + ", "
                    + stringValue);

        } catch (IOException e) {
            e.printStackTrace();
        }
    }
}
Open DataOutputStream → Write Integer → Write Double → Write String → Close Output Stream → Open DataInputStream → Read Integer → Read Double → Read String → Display Values → Close Input Stream

6. Java FilterOutputStream and FilterInputStream:
------------------------------------------------
FilterOutputStream and FilterInputStream are abstract classes that allow you to filter or modify the data while reading from or writing to streams. The concrete implementations, 
such as BufferedOutputStream , DataOutputStream , and ObjectOutputStream , extend FilterOutputStream .
| Class                  | Explanation                                                                  | Most Common Use                                                                |
| ---------------------- | ---------------------------------------------------------------------------- | ------------------------------------------------------------------------------ |
| **FilterOutputStream** | Filters or modifies output data before writing it to the destination stream. | Creating custom output streams and adding extra processing while writing data. |
| **FilterInputStream**  | Filters or modifies input data while reading from a source stream.           | Creating custom input streams and preprocessing data during reading.           |

import java.io.FileInputStream;     // Imports FileInputStream for reading from a file
import java.io.FileOutputStream;    // Imports FileOutputStream for writing to a file
import java.io.FilterInputStream;   // Imports FilterInputStream for filtering input data
import java.io.FilterOutputStream;  // Imports FilterOutputStream for filtering output data
import java.io.IOException;         // Imports IOException for exception handling
public class FilterStreamExample {
    public static void main(String[] args) {
        String content =
                "Hello, this is some content to write with additional formatting!";
        // Writing data using FilterOutputStream
        try (FileOutputStream fos =
                     new FileOutputStream("filtered_output.txt");
             FilterOutputStream fosFiltered =
                     new FilterOutputStream(fos)) {
            byte[] data = content.getBytes(); // Converts String to bytes
            fosFiltered.write(data); // Writes data to the file
            fosFiltered.write('\n'); // Adds a new line character
            System.out.println(
                    "Content written to the file with additional formatting!");
        } catch (IOException e) {
            e.printStackTrace();
        }
        // Reading data using FilterInputStream
        try (FileInputStream fis =
                     new FileInputStream("filtered_output.txt");
             FilterInputStream fisFiltered =
                     new FilterInputStream(fis) {}) {
            int data;
            System.out.println("\nReading content from file:");
            while ((data = fisFiltered.read()) != -1) {
                System.out.print((char) data); // Converts byte to character and prints
            }
        } catch (IOException e) {
            e.printStackTrace();
        }
    }
}

7. Java ObjectStream and Java ObjectStreamField: used for serializing and deserializing Java objects, respectively. They allow objects to be written to streams and read back later.
-----------------------------------------------
| Class / Interface      | Explanation                                                          | Most Common Use                                                      |
| ---------------------- | -------------------------------------------------------------------- | -------------------------------------------------------------------- |
| **ObjectOutputStream** | Serializes Java objects and writes them to a stream.                 | Saving objects to files, databases, or network streams.              |
| **ObjectInputStream**  | Deserializes Java objects from a stream.                             | Restoring previously saved objects.                                  |
| **Serializable**       | Marker interface that enables object serialization.                  | Allowing objects to be persisted or transferred.                     |
| **ObjectStreamField**  | Describes and controls the fields that participate in serialization. | Custom serialization and field management during object persistence. |

| Process             | Description                                                    |
| ------------------- | -------------------------------------------------------------- |
| **Serialization**   | Converting an object into a byte stream (`writeObject()`).     |
| **Deserialization** | Converting a byte stream back into an object (`readObject()`). |

import java.io.Serializable;          // Imports Serializable for object serialization
import java.io.ObjectOutputStream;    // Imports ObjectOutputStream for writing objects
import java.io.ObjectInputStream;     // Imports ObjectInputStream for reading objects
import java.io.FileOutputStream;      // Imports FileOutputStream for file writing
import java.io.FileInputStream;       // Imports FileInputStream for file reading
import java.io.IOException;           // Imports IOException for exception handling
// Serializable allows objects of this class to be converted into a byte stream
class Person implements Serializable {
    private String name;
    private int age;
    public Person(String name, int age) {
        this.name = name;
        this.age = age;
    }
    @Override
    public String toString() {
        return "Name: " + name + ", Age: " + age;
    }
}

public class ObjectStreamExample {
    public static void main(String[] args) {
        // Writing objects to file
        try (ObjectOutputStream oos =
                     new ObjectOutputStream(
                             new FileOutputStream("objects.dat"))) {  //"objects.dat" is simply a file name where serialized Java objects are stored.
            Person person1 = new Person("Alice", 30);
            Person person2 = new Person("Bob", 25);
            oos.writeObject(person1); // Serializes person1
            oos.writeObject(person2); // Serializes person2
            System.out.println("Objects written to the file successfully!");
        } catch (IOException e) {
            e.printStackTrace();
        }
        // Reading objects from file
        try (ObjectInputStream ois =
                     new ObjectInputStream(
                             new FileInputStream("objects.dat"))) {
            Person person1 = (Person) ois.readObject(); // Deserializes person1
            Person person2 = (Person) ois.readObject(); // Deserializes person2
            System.out.println("\nRead objects:");
            System.out.println(person1);
            System.out.println(person2);
        } catch (IOException | ClassNotFoundException e) {
            e.printStackTrace();
        }
    }
}


class Person implements Serializable {
    private String name;      // Name of the person
    private int age;          // Age of the person
    private String password;  // Sensitive field that should not be serialized
    // Specifies which fields should be serialized
    private static final ObjectStreamField[] serialPersistentFields = {
        new ObjectStreamField("name", String.class), // Serialize name field
        new ObjectStreamField("age", int.class)      // Serialize age field
    };
    // Custom serialization method
    private void writeObject(ObjectOutputStream out) throws IOException {
        // Creates a container for serializable fields
        ObjectOutputStream.PutField fields = out.putFields();
        // Stores the value of the name field
        fields.put("name", name);
        // Stores the value of the age field
        fields.put("age", age);
        // Writes only the specified fields to the stream
        out.writeFields();
    }
}

Java I/O (Hierarchy)
    │
    ├── Byte Streams
    │      ├── InputStream
    │      └── OutputStream
    │
    ├── Character Streams
    │      ├── Reader
    │      └── Writer
    │
    ├── Buffered Streams
    │      ├── BufferedInputStream
    │      └── BufferedOutputStream
    │
    ├── Data Streams
    │      ├── DataInputStream
    │      └── DataOutputStream
    │
    └── Object Streams
           ├── ObjectInputStream
           └── ObjectOutputStream
		   
| Java I/O Class / Interface | Explanation                                           | Most Common Use                                           |
| -------------------------- | ----------------------------------------------------- | --------------------------------------------------------- |
| **FileInputStream**        | Reads byte data from a file.                          | Reading binary files such as images, PDFs, and documents. |
| **FileOutputStream**       | Writes byte data to a file.                           | Writing binary data to files.                             |
| **BufferedInputStream**    | Reads data using an internal buffer.                  | High-performance file reading.                            |
| **BufferedOutputStream**   | Writes data using an internal buffer.                 | High-performance file writing.                            |
| **SequenceInputStream**    | Combines multiple input streams into a single stream. | Reading multiple files as one continuous stream.          |
| **ByteArrayInputStream**   | Reads data from a byte array in memory.               | Processing byte array data as an input stream.            |
| **ByteArrayOutputStream**  | Writes data to a byte array in memory.                | Creating dynamic byte arrays without file I/O.            |
| **DataInputStream**        | Reads primitive data types and strings from a stream. | Reading structured binary data.                           |
| **DataOutputStream**       | Writes primitive data types and strings to a stream.  | Writing structured binary data.                           |
| **FilterInputStream**      | Filters or modifies input data while reading.         | Custom input processing and stream decoration.            |
| **FilterOutputStream**     | Filters or modifies output data while writing.        | Custom output processing and stream decoration.           |
| **ObjectInputStream**      | Deserializes Java objects from a stream.              | Restoring previously saved objects.                       |
| **ObjectOutputStream**     | Serializes Java objects to a stream.                  | Persisting objects to files or networks.                  |
| **ObjectStreamField**      | Defines fields that participate in serialization.     | Custom serialization control.                             |
| **InputStream**            | Abstract superclass for byte input streams.           | Base class for all byte-reading streams.                  |
| **OutputStream**           | Abstract superclass for byte output streams.          | Base class for all byte-writing streams.                  |
| **Reader**                 | Abstract superclass for character input streams.      | Reading text data.                                        |
| **Writer**                 | Abstract superclass for character output streams.     | Writing text data.                                        |
| **FileReader**             | Reads character data from a file.                     | Reading text files.                                       |
| **FileWriter**             | Writes character data to a file.                      | Writing text files.                                       |
| **BufferedReader**         | Reads text efficiently using buffering.               | Reading text files line by line.                          |
| **BufferedWriter**         | Writes text efficiently using buffering.              | Writing text files efficiently.                           |
| **Console**                | Reads user input and passwords from the command line. | Command-line applications and secure password input.      |
| **Serializable**           | Marker interface that enables object serialization.   | Persisting and transferring Java objects.                 |

| Category              | Classes                                                                                                 |
| --------------------- | ------------------------------------------------------------------------------------------------------- |
| **Byte Streams**      | InputStream, OutputStream, FileInputStream, FileOutputStream, BufferedInputStream, BufferedOutputStream |
| **Character Streams** | Reader, Writer, FileReader, FileWriter, BufferedReader, BufferedWriter                                  |
| **Data Streams**      | DataInputStream, DataOutputStream                                                                       |
| **Object Streams**    | ObjectInputStream, ObjectOutputStream, ObjectStreamField, Serializable                                  |
| **Memory Streams**    | ByteArrayInputStream, ByteArrayOutputStream                                                             |
| **Filter Streams**    | FilterInputStream, FilterOutputStream                                                                   |
| **Special Streams**   | SequenceInputStream, Console                                                                            |

Console: The Console class in Java provides a simple way to interact with the console or command-line interface. It allows reading input and printing output directly to the console.
-------
import java.io.Console; // Imports Console class
public class ConsoleExample {
    public static void main(String[] args) {
        Console console = System.console(); // Gets the console object
        if (console != null) { // Checks if console is available
            String input =
                    console.readLine("Enter your name: "); // Reads user input
            console.printf("Hello, %s!%n", input); // Prints formatted output
        } else {
            System.out.println("Console not available."); // Displayed when console is unavailable
        }
    }
}

FilePermission: The FilePermission class in Java is used to represent the access rights to files and directories. It is a part of the security framework to control what resources a codebase can access.
---------------
| Action         | Description                         |
| -------------- | ----------------------------------- |
| **read**       | Allows reading a file.              |
| **write**      | Allows writing to a file.           |
| **execute**    | Allows executing a file or program. |
| **delete**     | Allows deleting a file.             |
| **read,write** | Allows both reading and writing.    |

import java.io.FilePermission;   // Imports FilePermission class
import java.security.Permission; // Imports Permission class
public class FilePermissionExample {
    public static void main(String[] args) {
        String filePath = "/path/to/file.txt"; // File path
        // Creates a permission object with read access
        Permission permission =
                new FilePermission(filePath, "read");
        // Checks whether read permission is implied
        if (permission.implies(
                new FilePermission(filePath, "read"))) {
            System.out.println("Read access granted.");
        } else {
            System.out.println("Read access denied.");
        }
    }
}

Writer: The Writer class is an abstract base class for writing characters to an output stream. It provides various methods to write character data to a destination, like a file or a network socket.
------
| Class          | Explanation                                                              | Most Common Use                                                           |
| -------------- | ------------------------------------------------------------------------ | ------------------------------------------------------------------------- |
| **Writer**     | Abstract base class for writing character data to an output destination. | Writing text data to files, network sockets, and other character streams. |
| **FileWriter** | Concrete implementation of Writer for writing text to files.             | Creating and updating text files.                                         |

import java.io.FileWriter; // Imports FileWriter for writing characters to a file
import java.io.IOException; // Imports IOException for exception handling
import java.io.Writer; // Imports Writer abstract class
public class WriterExample {
    public static void main(String[] args) {
        // Creates a FileWriter and refers to it using Writer
        try (Writer writer = new FileWriter("output.txt")) {    //The FileWriter class is a subclass of Writer that writes characters to a file.
            writer.write("Hello, Java!"); // Writes character data to the file
            System.out.println("Content written successfully!");
        } catch (IOException e) {
            e.printStackTrace();
        }
    }
}
Common Writer Implementations:
| Class              | Purpose                                        |
| ------------------ | ---------------------------------------------- |
| **FileWriter**     | Writes characters to a file.                   |
| **BufferedWriter** | Writes characters efficiently using buffering. |
| **PrintWriter**    | Writes formatted text data.                    |
| **StringWriter**   | Writes character data to a string buffer.      |

Writer writer = new Writer();  --> Invalid, above FileWriter() is valid

Reader: The Reader class is an abstract base class for reading character data from an input stream. It provides various methods to read characters from a source, such as a file or a network socket.
------
| Class          | Explanation                                                          | Most Common Use                                                             |
| -------------- | -------------------------------------------------------------------- | --------------------------------------------------------------------------- |
| **Reader**     | Abstract base class for reading character data from an input source. | Reading text data from files, network streams, and other character sources. |
| **FileReader** | Concrete implementation of Reader for reading text from files.       | Reading text files.                                                         |

import java.io.FileReader; // Imports FileReader for reading characters from a file
import java.io.IOException; // Imports IOException for exception handling
import java.io.Reader; // Imports Reader abstract class
public class ReaderExample {
    public static void main(String[] args) {
        // Creates a FileReader and refers to it using Reader
        try (Reader reader = new FileReader("input.txt")) {
            int data;
            // Reads one character at a time until end of file
            while ((data = reader.read()) != -1) {
                System.out.print((char) data); // Converts int to character and prints
            }
        } catch (IOException e) {
            e.printStackTrace();
        }
    }
}

BufferedWriter and BufferedReader: Subclasses of Writer and Reader
---------------------------------
| Class              | Explanation                                                       | Most Common Use                                 |
| ------------------ | ----------------------------------------------------------------- | ----------------------------------------------- |
| **BufferedWriter** | Adds buffering to character output streams for efficient writing. | Writing large amounts of text data efficiently. |
| **BufferedReader** | Adds buffering to character input streams for efficient reading.  | Reading text files line by line efficiently.    |

import java.io.BufferedWriter; // Imports BufferedWriter for buffered writing
import java.io.BufferedReader; // Imports BufferedReader for buffered reading
import java.io.FileWriter;     // Imports FileWriter for writing to a file
import java.io.FileReader;     // Imports FileReader for reading from a file
import java.io.IOException;    // Imports IOException for exception handling
import java.io.Writer;         // Imports Writer abstract class
import java.io.Reader;         // Imports Reader abstract class
public class BufferedReaderWriterExample {
    public static void main(String[] args) {
        // Writing data using BufferedWriter
        try (Writer writer = new FileWriter("output.txt");
             BufferedWriter bufferedWriter = new BufferedWriter(writer)) {
            bufferedWriter.write("Hello, BufferedWriter!"); // Writes text to file
            System.out.println("Content written successfully!");
        } catch (IOException e) {
            e.printStackTrace();
        }
        // Reading data using BufferedReader
        try (Reader reader = new FileReader("output.txt");
             BufferedReader bufferedReader = new BufferedReader(reader)) {
            String line;
            System.out.println("\nReading content from file:");
            while ((line = bufferedReader.readLine()) != null) {
                System.out.println(line); // Reads and prints one line at a time
            }
        } catch (IOException e) {
            e.printStackTrace();
        }
    }
}

CharArrayReader and CharArrayWriter: Subclasses of Writer and Reader
-----------------------------------
| Class               | Explanation                                                     | Most Common Use                               |
| ------------------- | --------------------------------------------------------------- | --------------------------------------------- |
| **CharArrayWriter** | Writes character data to an internal character array in memory. | Building and storing text data in memory.     |
| **CharArrayReader** | Reads character data from a character array in memory.          | Processing character arrays as input streams. |

import java.io.CharArrayReader; // Imports CharArrayReader for reading from a character array
import java.io.CharArrayWriter; // Imports CharArrayWriter for writing to a character array
import java.io.IOException;     // Imports IOException for exception handling
import java.io.Reader;          // Imports Reader abstract class
import java.io.Writer;          // Imports Writer abstract class
public class CharArrayStreamExample {
    public static void main(String[] args) {
        // Writing characters to an internal character array
        try (Writer writer = new CharArrayWriter()) {
            writer.write("Hello, CharArrayWriter!"); // Writes data to character array
            char[] data =
                    ((CharArrayWriter) writer).toCharArray(); // Converts content to char array
            System.out.println("Written Data:");
            System.out.println(data);
            // Reading characters from the character array
            try (Reader reader = new CharArrayReader(data)) {
                int ch;
                System.out.println("\nReading Data:");
                while ((ch = reader.read()) != -1) {
                    System.out.print((char) ch); // Reads and prints characters
                }
            }
        } catch (IOException e) {
            e.printStackTrace();
        }
    }
}